function remainderCalculator(numerator, divisor) {
  return numerator % divisor;
}

const numerator = 5;
const divisor = 9;

const result = "Remainder: " + remainderCalculator(numerator, divisor);
console.log(result);
